# 行情缓存（parquet + 元数据）—— 体积大且可重新拉取，不进仓库
# ⚠ 必须写 `.cache/*` 而不是 `.cache/`：
#   父目录被整个排除时，git 不会进去看，下面的 `!` 例外就永远不生效。
.cache/*
*.parquet

# 例外：MCP 参照样本要提交
# 它很小（几十根 K 线），而且是「确实通过官方 Binance MCP 取过数」的证据，
# 让任何人不装 MCP 也能复现 ⓪ 号闸门的演示。
!.cache/mcp_ref_*.json

# 绝不能提交的东西
.env
.env.*
*.key
*.pem
credentials*
secrets*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 输出
reports/
*.log
