# 参赛工作交接

> 这份文件的作用：**任何时候新开一个会话，读完这个就能接着干。**
> 最后更新 2026-09-03

## 比赛信息

| | |
|---|---|
| 赛事 | Binance Agent OS Mini Hackathon |
| 赛道 | **A 组**：用 Agent OS 构建 AI 代理（$20,000 USDC） |
| 截止 | **2026-09-08 23:59 UTC**（北京时间 9 月 9 日 07:59） |
| 提交物 | 视频/演示 + GitHub 仓库 |
| 参赛动作 | 关注 @binance → 转发比赛帖 → 回复/引用转发并附提交内容 → 填问卷 `app.binance.com/uni-qr/user-su` |
| 仓库 | https://github.com/junchihe658-ops/truth-serum |

**没选 B 组（$40,000，连 MCP 实盘交易）的原因**：Binance MCP **没有测试网，只对接实盘**。
而作品作者的上一套交易系统在停机前 12 小时还在产生无止损裸仓
（`open_position` 不校验成交，造出交易所上不存在的幽灵持仓）。
6 天时间 + 刚修好的执行层 + 零真金实盘经验 = 不该拿真钱试错。

## 作品定位

**Truth Serum —— 一个不肯对你撒谎的回测审计器。**

所有参赛作品都会展示漂亮的回测数字。我们展示**那些数字是怎么假的**。

四道闸门，每一道都必须先证明自己能抓到人为植入的 bug，否则拒绝输出结论。

## 已完成

- [x] 审计框架（`audit.py`）：`Audit` 基类强制自检先于结论
- [x] 四道闸门：前瞻 / 重叠 / 本底 / 组合层
- [x] 合成数据冒烟（`examples/smoke.py`）
- [x] 真实币安行情接入（`data.py`，公开 REST + parquet 缓存）
- [x] 真实数据三 case 验证（`examples/real_binance.py`）
- [x] Binance MCP 授权完成（**最小权限**：只开「读取 Agentic 账户和市场数据」，
      子账户不放钱）

### 真实数据上的结果（这是核心 demo 素材）

```
闸门顺序：① 前瞻  ② 重叠  ③ 本底  ④ 组合层

✅❌❌❌   A 诚实的 RSI 动量（教科书策略）
❌✅✅✅   B 偷看下一根收盘价
✅✅❌❌   C 全样本调参的三因子共振
```

- **B 最能说明问题**：它的账户收益 +72.23%/折、t=+4.02、7/8 盈利折**都是真的**
  ——因为作弊确实能赚钱。**只有 ① 抓到它**，②③④ 全部放行。
  这就是为什么前瞻必须排第一。
- **C 最阴险**：没偷看未来、没重叠灌水，①② 全过。只有 ③ 零假设本底拆穿它
  （块打乱 80 次，20 次超过真实值，p ≈ 0.259）。
- **A 最有说服力**：一个完全合理、毫无作弊的教科书策略，依然过不了 ②③④。

## 待办

按建议顺序。**截止 2026-09-08 23:59 UTC**（北京 09-09 07:59）。

- [x] **Truth Serum 自己做成 MCP server**（`9d98a19`）—— `claude mcp add truth-serum`
      即可装进任何客户端，对着自己的策略跑审计
- [x] 报告输出 HTML —— `truthserum/report_html.py`，单文件、零外部依赖
- [x] **自然语言 → 策略代码** —— `truthserum/nl.py`，确定性解析器，
      MCP 工具 `strategy_vocabulary` + `audit_plain_language`。
      **不调 LLM、不执行外部代码**：代码由解析器生成，只可能用到词汇表里的算子。
      解析不了就明确拒绝并指出是哪几个字，绝不猜
- [x] **⓪ 号闸门的三个正确性缺陷**（一并修掉）：
      ① 没有参照样本时判成「检测器不可信」—— 没东西可查 ≠ 检测器坏了，
         改判「未检」（这会让合成数据的 `examples/smoke.py` 直接失败）
      ② 参照与缓存**没有重叠时段**时返回「0 根全对 ✅」——
         一道专抓「数字骗人」的闸门自己报了个骗人的数字
      ③ 覆盖不全仍显示「未见异常」—— 改判「未检」并写明覆盖率
      同时用官方 Binance MCP 补齐 BTC/BNB/SOL 参照，现覆盖 4/4 标的、91 根全对
- [x] **demo 剧本** —— `examples/demo.py`，四幕：一句话 → 大多数人会得到的
      漂亮数字 → 五道闸门 → 差在哪。配套 `truthserum/naive.py`（**故意写错的**
      天真回测器），所以「宣传的数字」是真算出来的，不是编的
- [x] **README 定稿** —— 含 MCP 安装、自然语言层、五道闸门、三个真实案例、
      `requirements.txt`（原先 README 让人跑一条不存在的 pip 命令）
- [ ] **录视频**（照着 `examples/demo.py` 的四幕讲）
- [ ] 自然语言层的报错措辞：一句话里同时「指标不认识」+「没有动作词」时，
      只报后者（动作检查在前）。信息不假但不够准，`MACD 金叉就满仓梭哈` 即是
- [ ] 用户完成 X 提交动作（关注、转发、带视频+链接回复、问卷）

**已决定不做**：把数据源整体切到 `BinanceMCP`。⓪ 号闸门「本地行情 vs 官方 MCP
逐根比对」比「直接用 MCP 取数」叙事更强 —— 两个 MCP 协同、互相制衡 —— 而且
切过去要重跑全部缓存。注意 `fc09720` 做的是【拿 MCP 当核验参照】，**不是**换
数据源；报告页脚现在仍印 `binance-public`，这是事实、不是遗漏。

## 环境

```
python  C:\Users\何俊驰\AppData\Local\Programs\Python\Python311\python.exe
项目    C:\Users\何俊驰\truthserum
git     已配置 origin，凭据已缓存，可直接 push
MCP     binance-mcp-server，user 作用域，状态 Connected
缓存    ./.cache/*.parquet（已 gitignore）
```

⚠ **MCP 工具只在会话启动时加载。** 授权发生在当前会话之后，
所以要用币安 MCP 拉数据必须新开会话。

## 几条不要重犯的教训

1. **判据只能是账户余额。** AUC、单笔期望、相对排名 —— 这三样在开发过程中
   各骗过作者一次。
2. **检测器必须先证明自己能抓到已知 bug。** ① 号闸门的第一版就被自检判定失效
   （拿截断版和全量版比，两边都留了未来数据，偷看被 padding 抹平）。
3. **跑回归测试前先确认哪些测试会碰真实账户。** 2026-09-02 的教训：
   判据用「平时会不会失败」是错的，正确判据是「会不会碰真实账户」。
4. **权限按最小申请。** 币安授权时把交易/借贷/划转全关了，只留只读。
   一个讲「不要自欺」的工具，自己的权限申请也该是克制的。
