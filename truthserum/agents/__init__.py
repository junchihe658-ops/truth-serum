"""会自己找策略的 agent —— 也是给审计器准备的最好靶子

这里的 agent 不是用来赚钱的，是用来**演示自欺是怎么产生的**：
一个有目标、会迭代、只盯着单一指标优化的 agent，几乎必然会把自己
优化进选择偏差里。而它自己看不见这件事 —— 从它的视角，数字确实变好了。

所以每个 agent 都必须交出完整的 `SearchLog`。只交胜出者不交搜索过程，
⑤ 号闸门就无从判断，而那正是最常见的一种自欺。
"""
from .tuner import TunerAgent

__all__ = ["TunerAgent"]
